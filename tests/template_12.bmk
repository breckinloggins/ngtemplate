

This is an example of a \"CString\" and should be \'escaped\'
There are &quot;many&quot; things in HTML that should be escaped, like &lt; &amp; &gt;
There are &quot;many&quot; things in HTML that should be escaped, like &lt; &amp; &gt;

